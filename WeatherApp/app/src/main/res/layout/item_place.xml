<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="10dp"
    android:background="@drawable/round_corners_transparent"
    android:paddingTop="26dp"
    android:paddingBottom="26dp"
    android:paddingStart="14dp"
    android:paddingEnd="14dp"
    tools:background="@drawable/round_corners_place_day">

    <ImageView
        android:id="@+id/drag_handle"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:src="@drawable/ic_drag_handle"
        android:visibility="gone"
        android:clickable="true"
        android:scaleX="0.8"
        android:scaleY="1.1"
        android:contentDescription="@string/move"
        app:layout_constraintTop_toTopOf="@id/info_layout"
        app:layout_constraintBottom_toBottomOf="@id/info_layout"
        app:layout_constraintStart_toStartOf="parent"
        tools:visibility="visible"/>

    <LinearLayout
        android:id="@+id/info_layout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="2dp"
        android:layout_marginEnd="24dp"
        android:layout_gravity="center"
        android:orientation="vertical"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@id/drag_handle"
        app:layout_constraintEnd_toStartOf="@id/place_current_temp">

        <TextView
            android:id="@+id/place_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:maxLines="1"
            android:ellipsize="end"
            android:textColor="@color/white"
            android:textSize="18sp"
            android:layout_marginStart="6dp"
            tools:text="Tuscany, IT"/>

        <TextView
            android:id="@+id/temps"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/text"
            android:textSize="14sp"
            android:layout_marginStart="5dp"
            tools:text="23° / 14°"/>

    </LinearLayout>

    <TextView
        android:id="@+id/place_current_temp"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_marginEnd="2dp"
        android:textColor="@color/white"
        android:textAlignment="viewEnd"
        android:textSize="36sp"
        android:textStyle="bold"
        app:tint="@color/white"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/checkbox"
        tools:text="24°"/>

    <com.example.weatherapp.utils.CheckableImageView
        android:id="@+id/checkbox"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:padding="4dp"
        android:layout_gravity="center"
        android:src="@drawable/checkbox_state"
        android:visibility="gone"
        android:clickable="false"
        android:contentDescription="@string/selected"
        app:useMaterialThemeColors="false"
        app:layout_constraintTop_toTopOf="@id/place_current_temp"
        app:layout_constraintBottom_toBottomOf="@id/place_current_temp"
        app:layout_constraintEnd_toEndOf="parent"
        tools:visibility="visible"/>



</androidx.constraintlayout.widget.ConstraintLayout>
